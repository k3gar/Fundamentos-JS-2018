var nombre = 'Kevin', apellido = 'Garcia';

var nombreEnMayuscula = nombre.toUpperCase();
var apellidoEnMinuscula = apellido.toLowerCase();

var primeraLetraDelNombre = nombre.charAt(0);
var cantidadDeLetrasDelNombre = nombre.length();

var nombreCompleto = nombre + ' ' + apellido;

var `nombreCompleto`${nombre} ${apellido}` /* Comilla invertida nos permite interpolar variables */